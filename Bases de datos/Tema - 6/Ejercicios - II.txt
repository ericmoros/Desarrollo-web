1. CREATE TABLE EMPLEADOS3 AS SELECT * FROM employees WHERE department_id=100;
  A) No se trae las restricciones de la vieja tabla

2. CREATE TABLE DEPARTAMENTOS2 AS SELECT * FROM departments;

3. ALTER TABLE EMPLEADOS3 ADD (hobby VARCHAR(40), NHIJOS NUMBER(2,0));

4. ALTER TABLE EMPLEADOS3 ADD (EDAD NUMBER(3,0) NOT NULL);
  A) No se crea porque ya existen registros que no tienen datos en edad
     con lo que habría que dar un valor por defecto para solucionarlo.

5. ALTER TABLE EMPLEADOS3 ADD (EDAD NUMBER(3,0) DEFAULT 0 NOT NULL);
  A) Dar un valor por defecto al añadir la columna.

6. ALTER TABLE EMPLEADOS3 DROP (EDAD, NHIJOS);